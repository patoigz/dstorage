# DStorage

To get started clone this repo.

# Quickstart
Set your `INFURA_PROJECT_ID` and `PRIVATE_KEYS`

```
GANACHE - CLI
ganache-cli
```

```
NPM
npm install
truffle migrate --reset
npm run start
```
